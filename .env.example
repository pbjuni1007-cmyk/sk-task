OPENAI_API_KEY=
PURCHASE_MODEL=gpt-4o-mini
LANGSMITH_TRACING=false
# Shopping uses local verified snapshots; no shopping API credentials required.

# Optional fallback for primary authentication/quota/rate-limit errors.
OPENAI_API_KEY_SUB=
